# Installed to /etc/systemd/zram-generator.conf by install.sh if missing.
# Compressed RAM swap — helps a lot on 8–16 GB machines.

[zram0]
zram-size = min(ram / 2, 6144)
compression-algorithm = zstd
swap-priority = 100
