# Upstream dotfiles reference

Folder **`angelofallars-dotfiles/`** is gitignored. Refresh or clone:

```bash
cd reference
git clone --depth 1 https://github.com/angelofallars/dotfiles.git angelofallars-dotfiles
# or: cd angelofallars-dotfiles && git pull
```

This repo’s Hyprland/Waybar/Dunst/Ghostty/tmux/Nvim setup was partially derived from there; diff against your tracked dotfiles when upstream changes.
